spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/stats_repository
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        current_session_context_class: thread
        dialect: org.hibernate.dialect.PostgreSQL10Dialect

logging:
  level:
    root: info